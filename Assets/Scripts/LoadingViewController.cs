//Place where are loading player saves and other configuration files
using UnityEngine;

public class LoadingViewController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Application.LoadLevel(1);
	}
}
